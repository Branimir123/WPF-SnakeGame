using SnakeGame.GameObjects.Common;

namespace SnakeGame.GameObjects.Snake
{
    public class EnemySnake : Snake
    {
        public EnemySnake(Position pos, int size, int speed)
            : base(pos, size, speed)
        {

        }
    }
}
